# 项目运行指南

本指南将帮助您快速启动并查看项目效果。

## 📋 前置要求

- Python 3.9 或更高版本
- Node.js 16 或更高版本
- npm 或 yarn 包管理器
- DeepSeek API Key 或 OpenAI API Key（二选一即可）

## 🔧 第一步：配置环境变量

在 `backend` 目录下创建 `.env` 文件，配置 API 密钥：

```bash
# 在 backend 目录下创建 .env 文件
cd backend
```

创建 `.env` 文件，内容如下：

```env
# DeepSeek API Key（推荐）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 或者使用 OpenAI API Key
# OPENAI_API_KEY=your_openai_api_key_here

# 默认使用的模型（可选，默认为 deepseek-chat）
DEFAULT_MODEL=deepseek-chat
```

**获取 API Key：**
- DeepSeek: https://platform.deepseek.com/
- OpenAI: https://platform.openai.com/

## 🚀 第二步：启动后端服务

### 1. 安装 Python 依赖

```bash
cd backend
pip install -r requirements.txt
```

**注意：** 如果使用虚拟环境（推荐）：
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate

# 然后安装依赖
pip install -r requirements.txt
```

### 2. 启动 Flask 服务器

```bash
python app.py
```

后端服务将在 **http://localhost:5000** 启动。

您应该看到类似以下输出：
```
 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

## 🎨 第三步：启动前端服务

### 1. 安装前端依赖

打开**新的终端窗口**（保持后端服务运行），执行：

```bash
cd frontend
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

前端服务将在 **http://localhost:3000** 启动。

您应该看到类似以下输出：
```
  VITE v4.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

## 🌐 第四步：访问应用

1. 打开浏览器
2. 访问：**http://localhost:3000**
3. 您将看到聊天占卜偶像的界面

## ✅ 验证运行状态

### 检查后端 API

在浏览器中访问：http://localhost:5000/api/idols

如果看到 JSON 格式的偶像列表，说明后端运行正常。

### 检查前端

访问 http://localhost:3000，应该能看到：
- 偶像选择页面
- 聊天界面
- 占卜功能

## 🐛 常见问题

### 1. 后端启动失败

**问题：** `ModuleNotFoundError: No module named 'flask'`

**解决：** 确保已安装所有依赖
```bash
cd backend
pip install -r requirements.txt
```

### 2. API 调用失败

**问题：** `LLM调用失败` 或 `401 Unauthorized`

**解决：** 
- 检查 `.env` 文件是否在 `backend` 目录下
- 确认 API Key 是否正确
- 确认 API Key 是否有足够的余额

### 3. 前端无法连接后端

**问题：** 前端显示网络错误

**解决：**
- 确认后端服务正在运行（http://localhost:5000）
- 检查浏览器控制台是否有 CORS 错误
- 确认 `vite.config.js` 中的代理配置正确

### 4. 端口被占用

**问题：** `Address already in use`

**解决：**
- 后端：修改 `backend/app.py` 中的端口号
- 前端：修改 `frontend/vite.config.js` 中的端口号

## 📝 快速启动命令总结

**终端 1 - 后端：**
```bash
cd backend
pip install -r requirements.txt
python app.py
```

**终端 2 - 前端：**
```bash
cd frontend
npm install
npm run dev
```

**浏览器：**
访问 http://localhost:3000

## 🎯 下一步

- 查看 [项目概述](project-overview.md) 了解项目架构
- 查看 [API 参考](api-reference.md) 了解接口详情
- 查看 [流程图](flow-chart.md) 了解业务流程

